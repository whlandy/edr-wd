# EDR-WD

# ── Runtime artifacts (must never be committed) ──────────────────────────
*.log
*.log.*
logs/
target/logs/
target/server.log
target/server.pid

# Local config (contains real passwords / credentials)
config/targets.local.json
config/targets.json
config/test_machines.json
target/config.json
target/config/test_machines.json
.env
.env.*
*.local.json
*.secret
*.secrets

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# OS
.DS_Store
Thumbs.db

# Test
.pytest_cache/

# Local scratch copies; runtime target scripts belong under target/scripts/
scripts/macos/

# Local macOS helper build artifacts
target/scripts/macos/click_security_center
