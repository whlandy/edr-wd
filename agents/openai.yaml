interface:
  display_name: "EDR-WD"
  short_description: "Operate and test cross-platform EDR automation"
  default_prompt: "Use $edr-wd to inspect, operate, repair, or test an EDR-WD Windows/macOS target workflow."

policy:
  allow_implicit_invocation: true
