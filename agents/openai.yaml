interface:
  display_name: "EDR-WD"
  short_description: "Configure, operate, trace, and test EDR automation"
  default_prompt: "Use $edr-wd to configure, operate, automate, trace, report, or test an EDR-WD Windows/macOS target workflow."

policy:
  allow_implicit_invocation: true
