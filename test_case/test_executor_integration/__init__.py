"""Stub integration harness marker module.

The real tests live alongside this file. P1.2 keeps the integration
suite as a single file under `test_executor_integration/`; future
checkpoints may add a `conftest.py` here when shared fixtures are
introduced."""

from __future__ import annotations