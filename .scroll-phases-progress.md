# edr-wd scroll system — review-fix phase progress

Repo: /Users/edr-test/edr-wd/  (git repo, branch hermes-remote)
Workspace: /Users/edr-test/Desktop/edr-wd/ (no git — do not git init/commit here)

Test command (system python3, NOT .venv):
  cd /Users/edr-test/edr-wd && python3 -m pytest test_case/test_scroll/ -q
  cd /Users/edr-test/edr-wd && python3 -m pytest -q   # full suite

Status: ALL phases f3-f8 COMPLETE via batch run (user elected option 2).
Full suite: 1095 passed, 611 deselected (was 1064 before this batch).
Scroll suite: 135 passed. Changes left UNCOMMITTED for user review (commit-gate).

Phases (all done):

- [x] f3  P0-3: real MCP scroll_region entry + main chain switch.
        target/scroll/backend.py (ScrollBackendSource + run_scroll_region),
        target/server.py (scroll_region MCP tool), test_case/test_scroll/test_backend.py.
- [x] f4  P1-4: has_next_page requires an ENABLED owner. detect.py.
- [x] f5  P1-5: region-scoped verification (PageVerifier.region) + bounded
        polling/backoff (ScrollCoordinator.step polls to policy.max_attempts).
        verifier.py + tests.
- [x] f6  P1-6: unified execution — machine (state_machine.run) drives, the
        ScrollCoordinator executes, via machine_step_fn + NavigationContext.detection.
        state_machine.py + test_f6_unification.py.
- [x] f7  P2: tightened detection rules — removed bare `>`/`<`/`more` substrings;
        chevron-only exact match + standalone-'more' token; fixed FLAT branch
        dropping prev_controls. detect.py + tests.
- [x] f8  Final: full regression green (1095 passed); consolidated review below.

Consolidated self-review for this batch:

✅ f3 — backend adapter + MCP tool. No fabricated results: backend-unavailable
   returns NOT_DISPATCHED, never a fake success. Tests: 8.
✅ f4 — disabled next button no longer reports has_next_page (owner-gated).
✅ f5 — region-scoped verify + bounded poll (exactly max_attempts polls). Tests: 4.
✅ f6 — machine drives, coordinator executes, detection relayed. Tests: 3.
✅ f7 — breadcrumbs "a > b", "moreinfo" no longer false positives; chevron-only
   buttons still detected; also fixed a latent FLAT-branch bug that dropped
   prev_controls. Tests: 6.
⚠️ Open items for human review (NOT blockers, per scope):
   - server.py scroll_region is wired but not exercised against a live Windows
     backend (no real E2E; genuine E2E would be test_scroll_e2e).
   - TREE_LAZY / virtual-markers contract was only partially tightened (f7
     focused on >/< / more false positives); TREE_LAZY detection refinement
     explicitly deferred (P2).
   - scroll_and_verify.py (old PR1 composite) still exists alongside the new
     chain; f6 unified the *machine + coordinator* but did not delete the old
     composite (kept for backward-compat; a follow-up could remove it once the
     scroll_region path is the only caller).

NO auto-commit was performed. Per-commit suggestions are in the review below.

=== P1 REVIEW RESPONSES (2026-08, user code review) — all merged ===
1. (P1-1) Tightened server.py scroll_region exception handling: removed the
   blanket `except Exception -> ok:false` mask. run_scroll_region already
   converts expected domain outcomes (BackendUnavailable, no controls) into
   honest NOT_DISPATCHED; genuine bugs now propagate for the MCP framework to
   log the true traceback. Detection count moved to 0 blanket catches.
2. (P1-2) Added test_case/test_scroll/test_server_scroll_region_contract.py
   (4 tests) proving the REAL MCP entry path (server.scroll_region ->
   run_scroll_region -> detect -> plan -> dispatcher -> verify) is wired to the
   NEW architecture — prevents re-bypass to _backend.scroll(). This test
   surfaced a real production robustness bug: a scroll_region() call with no
   explicit identity prepended an all-empty window target and crashed
   build_snapshot with ProtocolModelError. Fixed ScrollBackendSource.targets()
   to derive window identity (title/native_window_id/process_name) from the
   dump_tree top-level metadata and stamp it onto controls; added
   test_targets_default_identity_drives_window_meta_from_dump_tree. Full suite
   now 1106 passed (was 1095).
3. (P1-3) Added can_advance + has_available_next_page aliases on
   DetectionResult (has_next_page kept as backward-compat name).
P2 (defer, not this round): split ScrollBackendSource, hide pid/process_name
   behind a session layer, and per-phase merges f3-f8.
